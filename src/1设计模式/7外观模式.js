// 为子系统中的一组接口提供一个高层接口
// 使用者使用这个高层接口

function bindEvent(ele, type, seletor, fn) {
	if (fn == null){
		fn = selector
		selector = null
	}
	// ****

}

bindEvent(ele,'click', '#div', fn)
bindEvent(ele,'click', fn)